// Sentinel value for activeYearFrom meaning "all years" — set when the user
// explicitly clears the start-year filter. Owned by CourseApp; mirrored here so
// the header can tell "no filter" apart from a real year. See CourseApp.jsx.
export const ALL_YEARS = "0";
