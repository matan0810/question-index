// How many exam cards to reveal per "show more" step.
export const PAGE_SIZE = 6;

// Defaults used when no sort is chosen yet: newest exams first.
export const DEFAULT_SORT_BY = "date";
export const DEFAULT_SORT_DIR = "desc";
